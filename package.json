{
  "name": "springo",
  "private": true,
  "description": "Real-world bingo. Dependencies are for the Netlify functions only; the app itself has no build step.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0",
    "@netlify/blobs": "^8.1.0",
    "zod": "^3.23.8"
  }
}
